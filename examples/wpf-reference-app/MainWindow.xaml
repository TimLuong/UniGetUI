<Window x:Class="WpfReferenceApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WPF Reference Application - UniGetUI Patterns" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="WPF Reference Application"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Controls -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,20">
            <Button Content="Load Packages"
                    Style="{StaticResource ModernButton}"
                    Command="{Binding LoadPackagesCommand}"/>
            <Button Content="Clear Packages"
                    Style="{StaticResource ModernButton}"
                    Command="{Binding ClearPackagesCommand}"/>
            <Button Content="Test Task Recycler"
                    Style="{StaticResource ModernButton}"
                    Command="{Binding TestTaskRecyclerCommand}"/>
            <TextBlock Text="{Binding StatusMessage}"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"
                       FontStyle="Italic"
                       Foreground="Gray"/>
        </StackPanel>

        <!-- Package List -->
        <Border Grid.Row="2" 
                BorderBrush="LightGray" 
                BorderThickness="1"
                CornerRadius="4">
            <ListView ItemsSource="{Binding Packages}"
                      SelectedItem="{Binding SelectedPackage}"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" 
                                      Width="250"
                                      DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Version" 
                                      Width="100"
                                      DisplayMemberBinding="{Binding Version}"/>
                        <GridViewColumn Header="Source" 
                                      Width="150"
                                      DisplayMemberBinding="{Binding Source}"/>
                        <GridViewColumn Header="Description" 
                                      Width="350"
                                      DisplayMemberBinding="{Binding Description}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Background="#F5F5F5"
                Padding="10"
                Margin="0,20,0,0"
                CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{Binding InfoMessage}"
                           VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding PackageCount, StringFormat='Total Packages: {0}'}"
                           VerticalAlignment="Center"
                           FontWeight="Bold"/>
            </Grid>
        </Border>
    </Grid>
</Window>
